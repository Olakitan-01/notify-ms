A production-grade distributed notification system built with a microservices architecture.
The system handles email and push notifications through asynchronous message queues (RabbitMQ),
with each service independently deployable, fault-tolerant, and horizontally scalable.

Built with Node.js (Express), PostgreSQL, Redis, RabbitMQ, and Docker.
